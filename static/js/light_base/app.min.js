const header = document.getElementById("navBar"),
  logo = document.getElementById("primary-logo");
window.addEventListener("scroll", function () {
  const currentScrollTop = void 0;
  (window.pageYOffset || document.documentElement.scrollTop) >= 15
    ? (header.classList.add("bg-base-100", "shadow-xl"),
      logo.classList.add("text-primary"))
    : (header.classList.remove("bg-base-100", "shadow-xl"),
      logo.classList.remove("text-primary"));
}),
  showLottie("profile-icon-lottie");
  showLottie("verify-your-account-lottie");